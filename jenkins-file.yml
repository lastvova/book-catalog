pipeline {
  agent any
  stages{
    
    stage ('Build') {
      steps {
        mvn clean
        mvn package
        }
        post {
          success {
            echo "Archiving..."
            archiveArtifacts artifacts:'**/target/*.war'
          }
        }
      }
    
    stage ('Medium Build') {
      steps {
        echo "Build step..."
        }
      }
    stage ('Deploy') {
      steps{
        echo "Deploy step..."
        }
      }
    }
}
